\section{Introduction}
\label{sec:intro}

Causal attribution, determining which factors brought about a given outcome and to
what degree, is a central reasoning task in many important problems, such as
gauging the efficacy of medical treatments \citep{Shepherd2024}, pinpointing drivers
of economic growth \citep{CheshireMagrini2009}, root cause analysis in automated
systems \citep{Papageorgiou2022}, or assessing the impact of public policy
\citep{HeckmanPinto2022CausalPolicyAnalysis}. We may want to understand why an
adverse outcome occurred so as to prevent recurrence, or how a predicted positive
outcome could be brought about.

In the absence of scalable, formal causal attribution tools, analysts often resort to
strong correlation, historical comparison, domain expertise, or principled but
non-causal attribution methods such as Granger causality \citep{Granger1969} and SHAP
\citep{lundberg2017unified}. Causal attribution is subtle enough, and large models complex enough, that even researchers with an advanced command of statistical and experimental methods benefit from scalable automation \citep{Shepherd2024}.
Here we present a tool for accurate and tractable causal attribution applicable to a
wide array of causal, probabilistic, machine-learned models.

What would a satisfactory causal attribution method look like? At minimum, it
should be (i) \textit{causally grounded}, tracking genuine counterfactual
dependence; (ii) \textit{context-sensitive},%
\footnote{We use \emph{context-sensitive} in Halpern's sense: sensitivity to which
\emph{endogenous} mediators are held fixed at their factual values during a
counterfactual evaluation, the witness mechanism formalised in
Section~\ref{sec:causal_impact}. This is not the same as sensitivity to the exogenous
noise realisation $\mathbf{u}$ itself. Halpern's literature calls $\mathbf{u}$ a
\emph{context} only because his models are deterministic, so fixing $\mathbf{u}$
fixes every endogenous value outright and ``the context'' and ``the factual world''
coincide; once mechanisms are stochastic, as throughout this paper, that
coincidence breaks, and noise-realisation sensitivity becomes a separate,
comparatively uninteresting axis on which every counterfactual or actual-cause
query, ours included, trivially qualifies while a population-level interventional
query does not. What actually distinguishes \ourapproach{} and actual causality
from interventional measures is context sensitivity in the mediator-holding sense:
even a ``vanilla'' structural counterfactual, which recomputes every mediator
downstream of an intervention rather than holding any of them fixed, is not
context-sensitive in this sense.}
distinguishing
active and inactive causes in particular instances; (iii) \textit{broadly applicable}, handling stochastic models,
discrete and continuous variables, and finite (not just infinitesimal)
counterfactual changes; and (iv) \textit{tractable}, estimable from samples
without exhaustive enumeration over witness sets and alternatives.%
\footnote{This trades enumeration for Monte Carlo sampling, not for a lower
asymptotic cost: achieving a fixed estimator variance still requires a sample count
that scales with the cardinalities of the suspect and witness sets, and we do not
have a general way to know in advance when fewer samples suffice.
Section~\ref{sec:ac_benchmark} reports empirical scaling; Section~\ref{sec:discussion}
returns to this and other limitations.}
A further, stronger demand (which we flag but do not treat as a minimum bar) is a graded notion of causal explanation, developed in full in Section~\ref{sec:causal_impact}.

Existing methods each forfeit one or more of these. Actual causality
\citep{actualCausalityHalpern} is grounded (i) and context-sensitive (ii), but neither
broadly applicable (iii) nor tractable in general (iv), with no clear approximation
\citep{eiterComplexityResultsStructurebased2002}. Methods that \textit{do}
scale either give up causal grounding (i) or are restricted in scope (iii). SHAP
\citep{lundberg2017unified} grounds attribution in observational correlations,
so a feature can receive high attribution merely for being correlated with a
true cause; \citet{janzing2020feature} make essentially this point, and Causal
SHAP \citep{heskes2020causal} addresses it to some extent, though its attributions still conflict with the causal structure in important cases (Section~\ref{sec:shap_examples}).
\citet{butler2022differential}'s Differential Causal Effect (DCE) is causally
grounded (i) but local: it relies on gradients, so requires differentiability and
does not capture effects from non-infinitesimal changes in potential causes.%
\footnote{DCE is, fairly, not designed for responsibility attribution:
\citeauthor{butler2022differential} target rates of change of treatment.}
Standard treatment-effect quantities — the average treatment effect (ATE) and conditional average treatment effect (CATE) — are causally grounded (i) and
often tractable (iv) but lack context-sensitivity (ii): they hold neither the context
nor the intermediate outcomes fixed while varying the candidate cause, so an
intervention's effects propagate downstream through the structural equations rather
than respecting which causal paths were active in that instance
(Section~\ref{sec:motivations}).

\begin{table}[htbp]
\centering
\renewcommand{\arraystretch}{1.45}
\begin{tabular}{@{}l c c c c c c >{\columncolor{pcihl}}c @{}}
\toprule
 & \makecell{Actual\\causality} & SHAP & \makecell{Causal\\SHAP} & \makecell{PN/PS\\/PNS} & DCE & \makecell{ATE/\\CATE} & \textcolor{pciblue}{\textbf{\ourapproach}} \\
\midrule
(i) causally grounded    & \yes & \no  & \partialm & \yes & \yes & \yes & \pciyes \\
(ii) context-sensitive   & \yes & \no  & \no       & \no  & \na  & \no  & \pciyes \\
(iii) broadly applicable & \no  & \yes & \yes      & \no  & \no  & \yes & \pciyes \\
(iv) tractable           & \no  & \yes & \yes      & \yes & \yes & \yes & \pciyes \\
\bottomrule
\end{tabular}
\caption{Which of the four desiderata each attribution method satisfies. \partialm\ = partial; \na\ = not addressed by design.}
\label{tab:method_comparison}
\end{table}

Concretely: in a stochastic Old Boys' Club Bank (OBCB) that checks credit scores only
for male applicants, Alice, a woman with bad credit, is denied a loan without her
credit ever being evaluated. Actual causality, using its witness mechanism to hold
the (never-triggered) credit check fixed, correctly names her gender as the cause of
the denial; Pearl's PN/PS/PNS, lacking any witness mechanism, instead assigns her
credit comparable responsibility, despite it never having been checked.
Section~\ref{sec:motivations} works through this example, and its stochastic
counterpart, in full.

To overcome these tradeoffs, we introduce \emph{probabilistic causal impacts} (\Ourapproach), an attribution
framework that combines the context-sensitivity of actual causality with the
scalability of probability-of-causation methods. \Ourapproach generalises Pearl's
probability of necessary and sufficient causation \citep{pearl2022probabilities} from
binary to arbitrary variable domains,%
\footnote{\citet{kawakammi_2024_poc_for_cont_and_vec} independently extend PNS to
continuous outcomes, but via a different route: a monotonicity assumption on the
structural mechanism yields point identification from observational conditional
distributions alone. \ourapproach instead works from a fitted causal model via
simulation, requires neither monotonicity nor point identification, and adds the
context-sensitivity that the witness mechanism below provides; we return to this
contrast in Section~\ref{sec:other-prob-ac}.}
and incorporates the \emph{witness} mechanism from actual causality
\citep{actualCausalityHalpern}, but changes its logical form: Halpern's account asks
whether \emph{some} witness set, out of exponentially many, reveals the dependency;
\ourapproach instead asks how much of a $\Gamma$-weighted distribution over witness
sets reveals it, replacing an existential quantifier with an expectation.
Intuitively, a witness set records the variables held at their factual values while a
candidate cause is varied, so that causal paths inactive in the factual world stay
blocked rather than re-activating under intervention, delivering the
context-sensitivity that ATE and CATE forgo.

This one move is what makes \ourapproach tractable, and also where its main risk
sits. Existential search over witness sets forces AC's brute-force enumeration; an
expectation admits Monte Carlo sampling from $\Gamma$ instead. The cost:
$\Gamma$-mass on witness sets that do not reveal a dependency can swamp a single
witness set that does, or sampling can simply miss it if it is rare enough --- in the
worst case, as expensive to find as enumeration itself (Section~\ref{sec:discussion}).
Two facts limit the damage. Proposition~\ref{prop:filter} shows existence survives the
move exactly: the suspects with positive score are precisely Halpern's actual causes,
for any regular $\Gamma$; $\Gamma$ only sets the score's \emph{magnitude}. And that
magnitude is a different kind of quantity than a normality ranking: Halpern's own
graded extension \citep{Halpern2015-HALGCA} grades which alternative value is
typical, while $\Gamma$ grades which suspect-witness explanation is worth asking
about --- a parsimony prior over candidate explanations, not a typicality ranking
over outcomes. Since $\Gamma$ only sets magnitude, we read \ourapproach scores
comparatively throughout.

Several components have precedents (expectations over
exogenous noise \citep{actualCausalityHalpern}, weighting by alternative-value
probabilities \citep{Beckers2015CombiningPC}, and aggregation over witness sets
\citep{Beckers2015CombiningPC, actualCausalityHalpern}), but no prior construction
combines these components into a single tractable estimator
handling arbitrary variable domains.
Crucially, every quantity entering the \Ourapproach definition is a standard
interventional or counterfactual quantity; the estimands built from them, the
expectations that give \Ourapproach its score, are already approximated automatically
by causal probabilistic-programming systems such as ChiRho%
\footnote{\url{https://basisresearch.github.io/chirho/explainable_sir.html}}
for a broad class of probabilistic causal models, so \Ourapproach can be computed
without bespoke per-model derivation.

\ourapproach presumes access to a trained probabilistic causal model from which
counterfactual outcomes can be sampled, a stronger requirement than methods that
work from observational data and a causal graph alone (e.g., the probability-of-necessity/sufficiency (PN/PS/PNS) bounds of
\citet{muellerpearl2022causes}, which return \emph{intervals} on the probability of
causation where \ourapproach, given a fully specified model, returns a posterior distribution of kernel values, summarised as point scores via their means, or
Causal SHAP). Within that requirement, however, we place no strong restrictions on the form
of the model, distributions, or types of variables.
Such models can comprise standard machine learning components such as neural
network parametrized distributions or Gaussian processes; a feed-forward
probabilistic program of this kind is still a structural causal model, just one whose
structural equations happen to be parametrized by a neural network rather than
specified by hand.
\citet{xia2022neural} show that such neural causal models, being universal
approximators, are expressive enough to represent the same nonparametric
counterfactual quantities available to an explicitly specified structural model. This
does not by itself resolve non-identification: many mechanisms can fit the same
observed data equally well, and no amount of representational flexibility changes
that. We instead operate \ourapproach in a Bayesian setting that presumes access to a
well-calibrated posterior induced by the trained model. With that in hand,
non-identification manifests as posterior-predictive uncertainty, informed only by
the prior where the data are silent. \ourapproach is
an expectation over the counterfactual distribution the model induces, so it is
well-defined even where the data do not constrain parts of that
distribution, which then simply reflect the prior.

With this breadth of application, and a measure that exposes its design choices
(the variable selection distribution, the alternative-value distribution, and the
causal impact function) as explicit user-facing components instead of buried
defaults, \ourapproach can serve causal attribution and explanation needs for a
wide array of users and applications.

\paragraph{Paper structure.}
The paper proceeds in four phases, tracked by a recurring layered roadmap
(Figure~\ref{fig:roadmap_inspirations}) that highlights the current stage, with the supporting appendix material
collected in a separate band.
The first stage establishes motivation and formalism.
Section~\ref{sec:motivations} works through a running example
to draw out what actual causality, the probability of necessity/sufficiency,
and average/conditional treatment effects each get right and miss, motivating
the design choices behind \ourapproach.
Section~\ref{sec:causal_impact} then defines the method: a kernel built from a
sufficiency check at the factual configuration and a necessity check against
alternatives, integrated over a distribution of suspect/witness sets, with
three components given as user choices: the variable selection distribution
$\Gamma$, the alternative-value distribution $\Delta$, and the causal impact
function.

The second phase positions \ourapproach against existing attribution methods.
\cref{sec:shap_examples} compares \ourapproach with SHAP, Causal SHAP, ATE, and
CATE on two worked examples (OBCB, with two binary features, and a
signal-with-mediation model), tabulating which desiderata each satisfies or
fails, including cases where SHAP and even Causal SHAP track correlation rather than
counterfactual dependence.

The third phase establishes the formal relationship between \ourapproach and
actual causality. \cref{sec:relation_with_ac} shows that under specific choices of
$\Gamma$ and $\Delta$ the \ourapproach kernel is positive on a configuration
exactly when that configuration is an actual cause in Halpern's sense, so
approximate AC verdicts can be read off the kernel without enumerating witnesses.
\cref{sub:pearl_actual_cause_prob} positions \ourapproach against Pearl's
probability of actual causation: on the canonical desert-traveller example
\ourapproach recovers Pearl's within-scenario ranking with graded magnitudes, and
a weak-poison variant reveals two further responsibility intuitions
(path-reliability and cross-scenario reliability) that Pearl's binary indicator
don't express but \ourapproach's expectation does.

The fourth phase is an empirical and comparative evaluation.
\cref{sec:evaluation} reads the results at a glance and defers the full protocols to the appendices: \ourapproach
recovers the actual-causality literature's verdicts on overdetermination and
undercutting (\cref{sub:synthetic}); it diverges from gradient-based Differential
Causal Effect attribution where the local gradient misreads the counterfactual
contrast (\cref{sec:DCE}); it stays tractable where exact actual-cause enumeration
times out at $17$ variables, continuing to roughly $73$--$145$ variables above a
$0.85$ correct-attribution rate (\cref{sec:ac_benchmark}); it delivers a graded
comparison on a continuous-outcome dynamical SIR model, assigning lockdown roughly
twice the responsibility of masking where but-for reasoning cannot separate them
(\cref{sec:sir_benchmark}); and it scales to a deployed automated valuation model
with machine-learned components trained on millions of points, where SHAP
concentrates attribution on a few downstream variables while \ourapproach spreads
it across the upstream causal structure (\cref{sec:avm}).
\cref{sec:discussion} closes with discussion. We engage related work where it
bears on the argument. The broader literature on actual-causality-based
attribution and responsibility scores (probabilities of causation, degree of
blame and harm, and score-based explanations in databases and machine learning,
surveyed by \citet{bertossiAttributionScores2023}) comes up mainly in
\cref{sec:relation_with_ac,sub:pearl_actual_cause_prob} and the discussion.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/structure_map_01_motivations.pdf}
  \caption{\textbf{Roadmap of the paper.} An overview of the argument, here at its
  starting point. \ourapproach builds on two ideas: \emph{actual causality} and the
  \emph{probability of necessity and sufficiency} (top tier, highlighted as the current
  stage). From there the paper develops the motivations (\cref{sec:motivations}),
  defines \ourapproach (\cref{sec:causal_impact}), positions it against SHAP, Causal
  SHAP, PNS, actual causality, and the probability of actual causation
  (\cref{sec:shap_examples,sec:relation_with_ac,sub:pearl_actual_cause_prob}), and
  closes with an empirical overview (\cref{sec:evaluation}); the running-example
  computations and benchmark protocols are deferred to the appendices, to which that
  overview points.}
  \label{fig:roadmap_inspirations}
\end{figure}

\paragraph{Reproducibility.}
Every quantitative claim in this paper (the worked-example numbers, the
benchmark tables, and the figures) is reproduced in a set of companion
notebooks, rendered with their full outputs at
\url{https://rfl-urbaniak.github.io/explainable_paper/}. Throughout, we point to
the relevant notebook by name (for example \nb{obcb\_computations}); each name
links to its page on that site.




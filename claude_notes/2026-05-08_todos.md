# 2026-05-08 — Working TODOs

Punch list distilled from `notes/actions.md` + `reviews/reviews_structured.md`,
cross-checked against the current paper. We work through these one at a time.

Legend: `[ ]` open · `[~]` partial · `[x]` done · `[-]` dropped

---

## A. Hard gaps (likely rejection drivers)

- [x] **A1. Formalize Section 4 method as an algorithm.** *(done 2026-05-08)*
  The reviewer's "Section 4" was the AC benchmark estimator description, which
  lives in [sec8_ac_benchmark.tex](../sections/sec8_ac_benchmark.tex) now.
  Resolution: replaced the bullet-style "Approximate PCI estimator" prose at
  sec8:49–68 with `Algorithm 1` (algpseudocode) + an aligned per-line prose
  walkthrough. Same algorithm + prose mirrored into the
  `actual_causality_benchmark.ipynb` cell that previously held the six
  numbered points. Preamble: added `algorithm`/`algpseudocode` to main.tex.
  Compiles clean (50pp, no undefined refs).

- [x] **A2. Causal SHAP comparison block.** *(done 2026-05-08)*
  Determined that the four rebuttal points are largely already carried by
  the worked OBCB and signal-mediation walkthroughs in
  [sec4](../sections/sec4_examples.tex). Residual gap: Janzing and Sharma
  not cited. Resolution: Janzing inline at sec4:7 as the conceptual
  motivation for Causal SHAP; CF-Shapley (Sharma) added as a focused
  footnote at sec4:11 with three-point contrast (single reference vs
  $\Delta$, one-axis vs joint $(Y^s,Y^n)$, no witness mechanism). Bib
  entries added for both — Sharma flagged with a TODO note: verify
  authors/title/venue. Connection conjecture explicitly **not** added
  (too hand-wavy).

- [x] **A3. Sufficiency actually validated.** *(done 2026-05-08)*
  Reviewed the existing paper structure: sufficiency *is* exercised in
  sec5 (synthetic), sec6 (DCE), and sec9 (SIR). The AC benchmark uses
  necessity-only because AC has no sufficiency clause to compare against
  — already framed as a forced choice in
  [sec8](../sections/sec8_ac_benchmark.tex). Resolution: extended the
  scope-of-correspondence paragraph in
  [sec7:15–26](../sections/sec7_actual_causality.tex#L15-L26) and the
  matching pointer in the sec8 algorithm prose to cite sec5+sec6+sec9
  collectively as the venues where substantive sufficiency does real
  work, so reviewer skim-reading sec8 sees an explicit forward pointer.

- [x] **A4. Quantitative results in the real-world section.** *(done 2026-05-08 — partial; NDA-bound)*
  Resolution: rather than add quantitative results we cannot disclose,
  weakened and sharpened the framing in
  [sec10:1–11](../sections/sec10_avm.tex#L1-L11). The new opener makes
  disclosure constraints the *visible reason* for the lack of numbers,
  points the reader at the synthetic benchmark for quantitative
  validation, and makes explicit that what is reportable is the *shape*
  of the attribution distributions, not the features they assign to.
  The DAG topology figure and qualitative SHAP-vs-PCI divergence figure
  were already present.

## B. Reviewer-specific clarifications

- [x] **B1. Stone-throwing joint-support issue.** *(done 2026-05-08)*
  Folded a joint-support refutation into the existing closure-condition
  footnote on Theorem~\ref{th:ac-exp} in
  [sec7](../sections/sec7_actual_causality.tex). The footnote now (a)
  states the closure condition, (b) quotes the joint-vs-marginal-support
  distinction explicitly, (c) walks the $(S{=}0, H{=}1)$ chain example,
  (d) shows closure rules out exactly that case, and (e) anchors in the
  AC benchmark (suspects = roots, mediators = witnesses, so the suspect
  set is closure-respecting by construction).

- [x] **B2. Why theorems drop sufficiency.** *(done 2026-05-08)*
  Confirmed the rationale is already in the "necessity-only correspondence"
  paragraph at
  [sec7:15–26](../sections/sec7_actual_causality.tex#L15-L26): AC has no
  sufficiency clause; the kernel's sufficiency factor acts as a factivity
  check; the substantive sufficiency component is exercised elsewhere.
  Same edit as A3 extended the cross-references (sec5+sec6+sec9), making
  the explanation visible to a skim-reading reviewer.

- [x] **B3. Undefined terms sweep.** *(done 2026-05-08)*
  Audit found "existential necessity claim witness" and "reject-condition"
  already removed from current draft. Resolved residual "antecedent" /
  "active antecedent" → "suspect" / "active suspect" in 4 places (sec3:256,
  sec6:81, sec6:97, sec9:84). "blame" → "responsibility" in 3 places
  (sec1:110, sec8:20, sec8:211). The sec9 case had a real focal-vs-others
  distinction that "antecedents" was carrying — rephrased as "focal suspect
  / other active suspects in $\mathbf{C}\setminus\{X\}$" to preserve the
  distinction without introducing undefined vocab.

- [x] **B4. K selection criterion.** *(done 2026-05-08)*
  Resolution: added a footnote at
  [sec3:218](../sections/sec3_definitions.tex#L218) noting that SHAP makes
  the analogous decision tacitly — by averaging over all $2^{|N|}$
  coalitions, plain SHAP and Causal SHAP commit to $K=|N|$ as an unstated
  default; Shapley axioms force the choice rather than treat it as a
  tunable. PCI surfaces the same decision as an explicit knob.

- [x] **B5. "Why only upstream information."** *(done 2026-05-08)*
  Existing upstream-only paragraph at
  [sec3:300–318](../sections/sec3_definitions.tex#L300-L318) was already
  carrying the normality argument. Added a follow-up paragraph
  enumerating the three alternatives the rebuttal had promised — uniform
  (unrealistic, may be incompatible with $M$); fully-conditional (leaks
  the consequence back into $\Delta$); marginal (drops covariate
  dependence). The framework permits any of these, but upstream-only is
  the principled default.

## C. Narrative / accessibility

- [x] **C1. Running example threaded through the paper.** *(done 2026-05-08)*
  Confirmed via per-section grep that OBCB / Alice / Bob already woven
  through sec1 (1), sec2 (42), sec3 (60), sec4 (44) — entire theory half.
  Evaluation half (sec5+) uses different examples per section by design,
  each testing a different capability; forcing OBCB into them would be
  artificial. Light closing fix added in sec11 discussion: one-sentence
  bridge connecting OBCB's witness mechanism (D-A-rank for Alice) to the
  AVM upstream-vs-downstream attribution split.

- [-] **C2. Voter / powerset intuition paragraph.** *(dropped 2026-05-08)*
  Decision: skip. The formal fragment at sec3:281–285, the
  cardinality-sensitivity-analysis paragraph above it, and the new
  SHAP-tacit-default footnote (B4) together carry the load. A fourth
  treatment of the same point would read defensive. Reserve the voter
  narrative for a journal-version expansion if needed.

- [-] **C3. Continuous example as a tutorial with parallel SHAP.** *(dropped 2026-05-08)*
  Decision: drop for this revision. The signal-with-mediation example in
  sec4 already provides a continuous parallel SHAP / cSHAP / PCI walkthrough
  with desiderata table and Instance 2 baseline-collapse demonstration; the
  DCE example in sec6 covers continuous PCI in its own register. Adding a
  third consolidated tutorial would duplicate without sharpening.

## D. Code (notebooks — out of scope for tex pass)

- [ ] **D1.** SHAP and Causal SHAP implementations applicable to our models.
- [ ] **D2.** Notebook 1: basic categorical, AC vs SHAP vs cSHAP, with
  necessity/sufficiency variants.
- [ ] **D3.** Notebook 2: generalized stone-throwing, SHAP/cSHAP comparison.
- [ ] **D4.** Notebook 4: gradient-based attributions (optional SHAP/cSHAP).
- [ ] **D5.** Notebook 5: SIR benchmark (optional SHAP/cSHAP).
- [ ] **D6.** Notebook 6: harder example (TBD).

---

## Already in the paper (for the record)

- [x] ε-excised distribution motivation
  ([sec3:321–382](../sections/sec3_definitions.tex#L321-L382))
- [x] Limitation: requires full SCM
  ([sec11:85–120](../sections/sec11_discussion.tex#L85-L120))
- [x] Shapley / SHAP credit at K=|X|
  ([sec3:231](../sections/sec3_definitions.tex#L231),
  [sec4:68](../sections/sec4_examples.tex#L68))
- [x] Theorem-condition wording (alternative-intervention vs outcome support)
  ([sec7:105–110, 199–216](../sections/sec7_actual_causality.tex#L105-L110))
- [x] "Why not empirical distribution" — Bayesian motivation
  ([sec3:367](../sections/sec3_definitions.tex#L367))
- [x] Normality references retained
  ([sec1:45,49–52](../sections/sec1_intro.tex#L45),
  [sec3:310–315](../sections/sec3_definitions.tex#L310-L315))
- [x] L1 motivation for causal impact function
  ([sec3:526–530](../sections/sec3_definitions.tex#L526-L530))

---

## Suggested order

A1 → A2 → A3 → B2 → B1 → B3 → B4 → B5 → C2 → A4 → C1 → C3 → D*.

A1–A3 are tex-only and unblock the central rebuttal claim. A4 is heaviest
because it depends on what the commercial agreement allows us to show.

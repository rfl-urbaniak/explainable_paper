# Session note 2026-05-06 (2)

## Summary

Closed the open thread from
[2026-05-06 (1)](2026-05-06_1.md): dropped abduction from the signal-example
PCI computation. Sufficiency now uses prior noise (`var_eM=0.1` retained).
Updated `signal_mediation_computations.ipynb` end-to-end and rewrote the
PCI subsections of `sections/sec4_examples.tex`.

## Notebook changes (`docs/source/signal_mediation_computations.ipynb`)

1. **Sufficiency uses prior noise.** Cells 11, 13, and 18 (helpers) now
   compute $\bar c = \mathbb{E}[|B^n - b^\star|] - \mathbb{E}[|B^s -
   b^\star|]$ with sufficiency-world noise drawn from $P_\mathbf{U}$.
   `E_abs_N` patched to handle `sigma2=0` (returns `|mu|`).
2. **PCI is signed.** At Instance 3 (`x=-0.75, m=1, y=1`) DXY and DXM go
   negative; helpers and `desid_table` use `|R| > 0` for positive desiderata.
3. **Prose pass throughout.** Section 5/6 markdown rewritten without
   abduction references. Section 7 key findings, Sections 9–11 observations
   updated to "witnesses sharpen, not decide" framing.
4. **Trial of deterministic chain backed out.** Briefly tried `var_eM=0` to
   make W=∅ tie DMXY exactly (motivating witnesses as essential). Rafal
   preferred to keep noise. Reverted everything to `var_eM=0.1` and
   Instance 3 = `(-0.75, 1, 1)`.

## New numerical picture (Instance 1, $x{=}m{=}y{=}1$)

| Desid | Plain | Causal | PCI W=∅ | PCI W=3rd |
|---|---|---|---|---|
| DXY | 0.250 ✓ | 0.250 ✓ | **0.249 ✓** | **0.186 ✓** |
| DMY | 0.250 ✓ | 0.250 ✓ | **0.283 ✓** | **0.319 ✓** |
| DXM | 0.306 ✓ | 0.375 ✓ | **0.253 ✓** | **0.284 ✓** |
| DMXY | 0.25=0.25 ✗ | 0.25=0.25 ✗ | **0.283>0.249 ✓** (gap 0.034) | **0.319>0.186 ✓** (gap 0.133) |

DMXY passes at all three instances under both PCI configurations. The W=∅
gap is structural (sufficiency-variance asymmetry from $\varepsilon_M$) but
small; W=3rd amplifies it by ~3-4×.

## Paper changes (`sections/sec4_examples.tex`)

1. **PCI computation subsection (sec:pci_signal_computation, ~lines 566-624).**
   Dropped abduction language. Replaced sufficiency-zero block with per-pair
   $(\mathbb{E}[|Y^n-1|], \mathbb{E}[|Y^s-1|], \bar c)$ tuples. Final:
   $R(X\rightsquigarrow Y \mid \mathbf{W}{=}\{M\}) \approx 0.186$ (was 0.402).
2. **Desiderata table.** All 8 PCI cells updated. DMXY row: W=∅ now ✓
   (was ✗); W=3rd ✓ at $0.319 > 0.186$ (was $0.508 > 0.402$). Caption
   replaces "partial abduction" with "prior-noise sufficiency"; formula
   shows signed $\bar c$.
3. **Discussion (~lines 732-775).** Section heading: "PCI: structural
   intervention separates the paths; witnesses sharpen the gap."
   Acknowledges W=∅ separation is fragile (margin 0.034); witnesses
   amplify to 0.133 → DMXY becomes a *robust* attribution rather than a
   *marginal* one.
4. Compiles clean (`main.pdf`, 30 pages).

## Open / deferred

- Three-instance extension of paper tables (Table 1, Table 2 in
  `sec4_examples.tex` still show only Instance 1) still pending.
- The role-of-witnesses narrative in the paper now differs between OBCB
  (essential) and signal (sharpening). Worth checking that the
  cross-references in §4 closing paragraphs and §1 intro still read
  coherently with this two-mode framing.
- Section 4.2/4.3 numbers (PN/PS for OBCB) should be re-checked against
  the OBCB notebook, which also had abduction-related updates earlier
  this week.

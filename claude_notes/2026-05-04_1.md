# Session continuation note — 2026-05-04

## Persona in use

`/home/rafal/notes/claude_scripts/careful_writing.md` — ML Paper Writing Collaborator
(propose-and-confirm; refresh every 15 min and inform user). Refresh on each major work
block.

---

## High-level state of the paper

- Compiles clean on every change in this session.
- Template was swapped from `clear2026` to `article` + `\usepackage[preprint]{neurips_2024}`
  for arXiv release. `neurips_2024.sty` is in the project root.
- Bibliography style: `plainnat`. `bibtex main && pdflatex main && pdflatex main` works.
- Pre-existing missing-section warnings remain (deliberate placeholders): `sec:th_ac`,
  `sec:ac_benchmark`, `sec:con_benchmark`, `sec:avm`, `app:DCE`, `app:conceptual`,
  `sec:TODO`. All pre-existing; not introduced this session.

---

## Edits applied this session

### Section 4 (Examples and comparison to SHAP/Causal SHAP)

1. **Added Section 4.6 — Desiderata analysis for the signal example.**
   Includes 7-row table (DXY, DMY, DXM, DYX, DYM, DMX, DMXY) plus four
   discussion paragraphs. Both methods pass DXY/DMY/DXM. Causal SHAP fixes DYX only.
   DYM/DMX/DMXY remain failures.
2. **Added D-ind desideratum and corresponding section 7.1 example** (X=0.5, M=0.5, Y=1
   instance where φ=0 for all features). D-ind row in the table; discussion paragraph
   covering both the standard instance (Target X residual 0.143) and the X=0.5 instance.
3. **Section 4 intro and related-work paragraph** (lines ~207–220): SHAP critique split
   into clean two-sentence form; DCE concession lifted out of parenthetical;
   `section XXX` → `Section~\ref{sec:shap_examples}`; `\citep{causalSHAP}` →
   `\citep{heskes2020causal}` (key fix); `token-level causal attribution` →
   `attributing causes to specific outcomes`; intractability sentence shortened to
   eliminate overfull.

### Introduction polish

4. **Mechanistic reasoning** dropped from the ad-hoc list (was self-undercutting).
5. **`\textit{truly} \textit{causal}`** → plain `causal` (marketing-y phrasing).
6. **Item 5 of structure list** softened: "find that our approach yields explanations
   which agree with domain expert opinion, while SHAP does not" → "compare to SHAP and
   discuss the differences in the resulting explanations." Marked for revisiting once
   AVM section is finalised.
7. **SCM-availability assumption** added at the start of the contributions paragraph:
   "PCI presumes access to a trained probabilistic causal model from which counterfactual
   outcomes can be sampled --- a stronger requirement than methods that work from
   observational data and a causal graph alone (e.g., PN/PS/PNS bounds, Causal SHAP)."
8. **"Turnkey nature of our measure"** reframed as "a measure that surfaces its design
   choices --- the variable selection distribution, alternative value distribution, and
   causal impact function --- as explicit user-facing components rather than hidden
   defaults."

### Normality reference (was a withdrawn motivation per reviewer)

9. **Intro paragraph 3** — replaced the "Inspired by descriptive normality" gesture with
   an operational link: "Our construction makes partial contact with the study of
   *descriptive normality* ... a concrete, if partial, operationalization of the
   normality intuition."
10. **New paragraph after the Δ definition** (sec:causal_impact area): "When
    $\Delta(\mathbf{x})$ is a posterior conditioned on upstream factual values --- the
    construction we use throughout --- alternatives are weighted by their plausibility
    under the structural model with upstream context held fixed. ... PCI does not aim to
    resolve the counterexamples motivating that line of work."

### Section 2 (Motivations) cleanup

11. **PN walkthrough rewritten** with itemize + multline*: factual/counterfactual setup,
    three branches (intervention propagates to `check`; checked→bad→0.05; not
    checked→loan=0), combined: 0.9 · 0.05 = 0.045.
12. **PS walkthrough added** (parallel structure to PN): names the additional conditioning
    convention explicitly ("conditions on Alice's credit"), then itemize + multline*
    showing PS_gender(Alice) = 1.
13. **Two `\begin{comment}...\end{comment}` blocks removed** (obsolete table + old PN/PS
    interpretation draft).
14. **PS definition fixed** — undefined `y'` replaced with `Y \neq y^\star`; prose
    updated to "neither $C=c^\star$ nor $Y=y^\star$ occurred"; where-clause untangled.
15. **Table `tab:pn_ps_pns` extended** to include both Population and Individual rows
    for both gender and credit (Alice and Bob columns each). 2-row × 2-col → 4-row × 2-col
    structure with explicit Level column.
16. **PNS prose simplified** — population-level numerical values dropped (now in the
    table), pointer added.

### Section 3 (Definitions)

17. **PSCM scope-limitation footnote trimmed** (lines 629+): dropped the redundant
    SHAP/Causal SHAP/PNS comparison block (now duplicates the intro), kept the F/P_U
    framing + Bayesian likelihood/prior analogy + sampling-as-routine-inference point.
    Two paragraphs → one.

### Template swap (clear2026 → NeurIPS preprint)

18. `\documentclass[anon,12pt]{clear2026}` → `\documentclass{article}` +
    `\usepackage[preprint]{neurips_2024}`.
19. Added `\usepackage{amsmath,amsthm}` plus `\newtheorem{theorem}/{definition}/{example}`
    declarations (NeurIPS doesn't provide these; clear2026 did).
20. `cleveref` moved to load AFTER `hyperref` (NeurIPS requires this order).
21. `\thicksim` → `\sim` (3 occurrences; `\thicksim` was clear2026-provided).
22. `\title[short]{long}` → `\title{long}`.
23. `\usepackage{times}` removed (NeurIPS sets fonts itself).
24. `\bibliographystyle{plainnat}` added before `\bibliography{references}`.
25. `\begin{keywords}...\end{keywords}` → "**Keywords:**" line at end of abstract.
26. **Author block populated with 5 of 8 authors**: Rafal Urbaniak, Eli Bingham, Poorva
    Garg, Jack Feser, Daniel Waxman. **TODO comment in source: add Andy, Sam, Drew (full
    names, emails, affiliations).**

---

## Pending / open items

### Author block
- Need full names, emails, affiliations for **Andy**, **Sam**, **Drew**. Marked with
  `% TODO` next to the `\author{...}` block.

### Sufficiency experiments
- `for_later.md` already flags this. Section 3 has a `\todo{Add cross-reference to
  sufficiency experiments once written}` at the closing of the `ci`-discussion paragraph.

### Cross-references to unwritten sections
- `sec:th_ac`, `sec:ac_benchmark`, `sec:con_benchmark`, `sec:avm`, `app:DCE`,
  `app:conceptual`, `sec:TODO` (used twice for continuous example).
- The structure list at the end of the introduction is wrapped in
  `\todo{revise this list once paper done}` — revisit when those sections exist.

### AVM section claim (intro item 5)
- Currently reads neutrally ("compare to SHAP and discuss the differences"). Reviewer
  S1iS specifically flagged AVM for lacking quantitative results. When the AVM section
  is finalised, restore (with evidence) or rephrase the structure-list item accordingly.

### Bibliography minor
- `pearl2022probabilities` has empty `publisher` field — bibtex warning, easy to fix
  by adding `publisher = {...}` in `references.bib`.

### Style/structure observations not yet addressed
- The desiderata block in Section 3 (D-A1, …, D-comp) and the Section 4 desiderata
  (DXY, …, DMXY, D-ind) are both example-specific. Section 3's closing 5-property list
  is the closest thing to a generic statement. Worth noting if a reviewer asks for
  PCI's general guarantees.
- "Necessity worlds" / "sufficiency worlds" terminology is introduced at sec:causal_impact
  line ~736 but used only sparingly. Could be trimmed if not load-bearing — flagged but
  not actioned.

### `for_later.md` items (untouched this session)
- Cardinality range (J, K) sensitivity validation experiment.
- Joint support condition for theorems in `sec:th_ac`.
- Sufficiency experiments (see above).

---

## Useful pointers for next session

- `claude_notes/obcb_computations.ipynb` — has verified PCI numerics for Alice and Bob.
- `notes/shap_chapter.tex` — long-form scratchpad on plain SHAP, Causal SHAP, mediator
  dilution, direct/indirect decomposition, residual limitations. Some material already
  pulled into Section 4 (D-ind, signal example walkthrough). Mediator dilution and
  Heskes' direct/indirect decomposition are NOT in main.tex yet — candidates if you want
  to expand Section 4.6 further.
- `notes/shap_discussion_summary.pdf` — companion summary; section 7.1 (D-ind) was the
  most recent integration target.
- `reviews/reviews_structured.md` — full structured reviewer comments. Most Section 1–3
  concerns from S1iS and 2MT8 are now addressed in the paper.
- `notes/actions.md` — checklist of revision items, organised by topic.

---

## Compilation verified

`pdflatex main && bibtex main && pdflatex main && pdflatex main` produces a 21-page PDF
with no fatal errors and no overfull/underfull boxes attributable to this session's edits.
Pre-existing missing-section warnings remain (listed above).

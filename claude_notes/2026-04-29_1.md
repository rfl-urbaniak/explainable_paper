# Session continuation note — 2026-04-29

## What was completed this session

### Bibliography and compilation fixes
- Removed duplicate `\bibliographystyle{plain}` from main.tex (conflicted with `plainnat`
  injected by jmlr.cls); bibtex now runs clean
- Added four missing bib entries to references.bib:
  - `shapley1953` — Shapley (1953), "A value for n-person games", Princeton UP
  - `kruschke2018` — Kruschke (2018), Advances in Methods and Practices in Psych. Science
  - `cohen1988statistical` — Cohen (1988), Statistical Power Analysis, Erlbaum
  - `heskes2020causal` — Heskes et al. (2020), NeurIPS 33
- Changed `\citep{shapley1953value}` → `\citep{shapley1953}` at line 909 (same paper,
  unified keys)
- Compilation: clean at 21 pages; only expected placeholder `\ref{}` warnings remain

### OBCB threading — insertions 1 and 2 already applied (from prior wip commits)
- Insertion 1 (PSCM): already in file at lines 572–581
- Insertion 2 (Interventions): already in file at lines 590–592

### Insertion 3 applied — Causal Set / S,W,C,T
Placed between the sufficiency/necessity paragraph and "We expand an original model..."
Sets S={gender,credit}, W={check-failed} for both Alice and Bob; explains why the witness
makes credit's causal role visible for Alice. Compiled clean.

---

## Currently in progress: OBCB threading, insertions 4–8

### Design decisions confirmed this session
- **Table format** (insertion 6): use `tabular` with columns PNS / PCI (no witnesses) /
  PCI (with witnesses), rows Alice-gender, Alice-credit, Bob-gender, Bob-credit
- **Closing payoff** (insertion 8): table (desideratum | PCI ✓/✗ | PNS ✓/✗) with
  informal gloss and reference in text
- **Bob**: include Bob in insertions 3–8 (not Alice-only)
- **Γ choice**: the uniform default for OBCB (|S|=2) is the unique symmetric choice;
  gesture at deeper cardinality-range discussion (from actions.md) with a forward reference
  to the experimental section — do NOT over-commit to a specific label

### Insertions 4 and 5 — APPLIED

**Insertion 4** (after Cardinality example): Γ uniform over 3 subsets for OBCB;
gesture at cardinality-range design decision for larger models; forward ref to
experimental section.

**Insertion 5** (after ε-excised example): Δ trivial for binary OBCB (point masses);
notes continuous case is where ε matters.

**Also applied — systematic informal glosses after all definitions in Section 3:**
- Interventional Law: point mass / integrating over P_U recovers interventional distribution
- Causal Set: list of variable-value assignments; basic unit of intervention
- Variable Selection Distribution: moved gloss from after Cardinality example to after
  definition; OBCB application stays after example
- Alternative Value Distribution: support condition ensures genuine counterfactual departure

### CURRENTLY IN PROGRESS — Insertion 4 — drafted, NOT YET APPLIED (STALE — SEE ABOVE)
Placement: after Cardinality-Constrained Uniform Selection `\end{example}`, before
"Causal attribution hinges on questions such as..."

Approved text (verbatim):

> \medskip\noindent\textit{Informally, the variable selection distribution $\Gamma$
> encodes prior beliefs about which subsets of suspects are worth examining. A uniform
> distribution treats all suspect subsets as equally plausible; a cardinality-constrained
> choice focuses the search on interactions of a particular order, analogous to choosing
> which interaction terms to include in a regression.}
>
> For the OBCB analysis we use a uniform $\Gamma_s$ over the three non-empty subsets of
> $\mathbf{S}$: $\{\varname{gender}\}$, $\{\varname{credit}\}$, and
> $\{\varname{gender}, \varname{credit}\}$, each with probability $1/3$. With only two
> suspects there is no prior reason to privilege any combination, so the uniform choice is
> the unique symmetric default. $\Gamma_w$ is uniform over
> $\{\emptyset, \{\varname{check\text{-}failed}\}\}$, each with probability $1/2$.
> In larger suspect sets the choice of cardinality range becomes substantive: sampling
> uniformly from the full powerset yields an expected intervention size of
> $|\mathbf{S}|/2$, which spreads attribution across large coalitions and may make
> individual feature contributions harder to isolate; we return to this design decision in
> the experimental section.

### Remaining insertions — not yet drafted

**6. After Joint Necessity and Sufficiency Measure** — THE PAYOFF
- Explain P^s and P^n for Alice's gender; show witness on check_failed is the decisive
  ingredient
- Include tabular with numbers (see table below)
- Mention Bob too

**7. After Causal Impact Function definition**
- ci = PNS binary; connect to table numbers; brief

**8. Closing payoff — after Absolute Difference example**
- Table: desideratum | PCI (with witnesses) | PNS | brief gloss
- Verify all 7 desiderata; contrast PNS failure on D-A-rank

---

## PCI and PNS numbers (from notebook, verified)

| Person | Feature | PNS | PCI (no witnesses) | PCI (with witnesses) |
|--------|---------|-----|-------------------|---------------------|
| Alice | gender | 0.045 | 0.210 | **0.263** |
| Alice | credit | 0.180 | 0.240 | **0.199** |
| Bob | gender | 0.000 | 0.038 | **0.019** |
| Bob | credit | 0.860 | 0.228 | **0.119** |

## Desiderata verification (PCI with witnesses vs PNS)

| Desideratum | PCI (with witnesses) | PNS |
|-------------|----------------------|-----|
| D-A1: gender > 0 for Alice | 0.263 > 0 ✓ | 0.045 > 0 ✓ |
| D-A2: credit > 0 for Alice | 0.199 > 0 ✓ | 0.180 > 0 ✓ |
| D-A-rank: gender > credit for Alice | 0.263 > 0.199 ✓ | 0.045 < 0.180 ✗ |
| D-B1: gender ≥ 0 for Bob | 0.019 ≥ 0 ✓ | 0.000 ≥ 0 ✓ |
| D-B2: credit > 0 for Bob | 0.119 > 0 ✓ | 0.860 > 0 ✓ |
| D-B-rank: credit > gender for Bob | 0.119 > 0.019 ✓ | 0.860 > 0.000 ✓ |
| D-comp: Alice gender > Bob gender | 0.263 > 0.019 ✓ | 0.045 > 0.000 ✓ |

PNS fails only D-A-rank. PCI without witnesses also fails D-A-rank (0.210 < 0.240).
Only PCI with witnesses satisfies all seven.

---

## All review-driven gaps addressed (2026-04-29 end of session)

1. ✅ SCM scope limitation — footnote on PSCM def, compares to SHAP/causalSHAP/PN/PNS
2. ✅ ε-excision three-way trade-off — footnote extended with observational/conditional/excised
3. ✅ "What PCI gives you" properties list — 5-item enumeration before desiderata table
4. ✅ Sufficiency validation — OBCB tables cited as concrete validation; y*=0 explanation
5. ✅ Shapley equivalence addressed head-on — concession + causal integrand distinction +
   witness mechanism + forward ref to sec:shap_examples

## Known loose ends (carry forward)

- `\ref{sec:TODO}` ×2, `\ref{sec:th_ac}`, `\ref{sec:con_benchmark}`, `\ref{sec:avm}`,
  `\ref{sec:ac_benchmark}`, `\ref{app:DCE}`, `\ref{app:conceptual}` — all placeholder
  refs for sections not yet written; expected warnings
- `pearl2022probabilities` has empty publisher field in bib — pre-existing, low priority
- Cardinality-range design decision (Γ choice for large suspect sets) deferred to
  experimental section; forward reference added in insertion 4
- Sufficiency experiments: tracked in for_later.md; `\todo` placeholder at line 816
